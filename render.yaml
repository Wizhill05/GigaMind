services:
  - type: web
    name: gigamind
    env: python
    buildCommand: (cd frontend && npm install && npm run build) || true; pip install -r requirements.txt
    startCommand: uvicorn gigamind.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GIGAMIND_API_KEY
        sync: false
      - key: DB_PATH
        value: ./gigamind.db
      - key: ENVIRONMENT
        value: production
